def helper(low: int, high: int):
    """Вернет list(range(low, high+1))
    
    Ключевые аргументы:
    low: int -- левая граница отрезка
    high: int -- правая граница отрезка
    """

    return list(range(low, high+1))
